{
  "name": "circles",
  "private": true,
  "version": "0.0.6",
  "type": "module",
  "workspaces": [
    "./frontend"
  ],
  "scripts": {
    "tauri": "tauri",
    "lint": "yarn lint:js && yarn lint:css",
    "lint:fix": "yarn lint:js --fix && yarn lint:css --fix",
    "lint:css": "stylelint '**/*.{css,scss}' --ignore-path .gitignore",
    "lint:js": "eslint . --ext js,jsx,ts --ignore-path .gitignore"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.12",
    "@tree-company/eslint-config": "^0.3.2",
    "@tree-company/stylelint-config": "^0.3.2",
    "eslint": "^8.57.0",
    "prettier": "3.2.5",
    "stylelint": "^16.3.1"
  },
  "eslintConfig": {
    "extends": [
      "@tree-company/eslint-config/react"
    ]
  },
  "stylelint": {
    "extends": [
      "@tree-company/stylelint-config"
    ]
  }
}
